function toggleSort() {
    return sort.hidden ? sort.hidden = !1 : sort.hidden = !0, !1;
}

function toggleFilter() {
    for (var a = document.getElementById("sortType"), b = document.getElementById("sortOrder"), c = 0; c < radioBtn.length; c++) radioBtn[c].checked && c < 2 ? a.innerText = radioBtn[c].value : radioBtn[c].checked && c > 1 && (b.innerText = radioBtn[c].value);
}

if (document.getElementById("exercises")) var oneMinuteTimer = function() {
    function a() {
        e = 60, f = 0, j = !1, k.setAttribute("src", "img/reset.svg"), c(), d(), arc_interval = setInterval(d, 500), 
        num_interval = setInterval(c, 1e3);
    }
    function b() {
        clearInterval(arc_interval), clearInterval(num_interval), m.clearRect(0, 0, 174, 174), 
        j = !0, k.setAttribute("src", "img/play.svg"), l.nodeValue = "1:00";
    }
    function c() {
        switch (!0) {
          case e < 10 && e > 0:
            l.nodeValue = "0:0" + e;
            break;

          case 0 === e:
            clearInterval(num_interval), clearInterval(arc_interval), m.clearRect(0, 0, 174, 174), 
            l.nodeValue = "0:0" + e;
            break;

          case 60 === e:
            l.nodeValue = "1:00";
            break;

          default:
            l.nodeValue = "0:" + e;
        }
        e--;
    }
    function d() {
        m.clearRect(0, 0, 312, 156), g = 2 / 120 * f, h = 1.5 + g, i = h * Math.PI, m.beginPath(), 
        m.arc(156, 78, 82, 3.5 * Math.PI, i, !0), m.lineWidth = 7, m.strokeStyle = "orange", 
        m.stroke(), f += 1;
    }
    var e, f, g, h, i, j = !0, k = document.getElementById("toggle_btn"), l = (document.getElementById("timer"), 
    document.getElementById("n_timer").firstChild), m = document.getElementById("arc").getContext("2d");
    timer.onclick = function() {
        switch (j) {
          case !0:
            a();
            break;

          case !1:
            b();
        }
    };
}();

if (document.getElementById("list")) {
    var filter = document.getElementById("chordList").getElementsByTagName("nav"), sort = document.getElementById("sortOverlay"), radioBtn = sort.getElementsByTagName("input");
    sort.hidden = !0, filter[0].addEventListener("click", toggleSort, !1);
    for (var i = 0; i < radioBtn.length; i++) radioBtn[i].addEventListener("click", toggleFilter, !0);
}